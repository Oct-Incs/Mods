# Oct-Incs/Mods - 作業前に必ず読むこと

このリポジトリは複数ゲーム・複数MODを格納する構成。詳細は `README.md` を参照。

作業対象のMODが決まったら、そのMODフォルダ直下の `CLAUDE.md`(存在すれば)と
`HANDOFF.md` を必ず読むこと。MODごとの絶対厳守ルール(例:
`RimWorld/JP_Beasts/CLAUDE.md` の「Textures/には触れない」)はそちらに
まとまっている。
